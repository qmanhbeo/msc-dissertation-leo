# HANDOFF — `main.py` architecture refactor (DONE, MERGED)

Merged to `main` in `903ec67`. Branch `main-refactor` work complete:

- `06b4c34` registry-driven appendix wiring + C0 no-op fix
- `cfd8553` shared stage builders + unified concept track + single poststeps call
- `bec3a7d` single-model `--stage analysis`, drop `_run_analysis_only`, fix silent double-run
- `74d2199` warm-replay re-verification (green, single-run, correlations unchanged)

No remaining actions. `5_notes/scratch/` and `handoff-2.md` are scratch/other-session docs — not committed here.
