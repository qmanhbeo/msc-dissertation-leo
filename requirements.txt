# Core pipeline dependencies for dissertation reproducibility.
# environment.yml is the canonical build path; this file is a
# lightweight reference for manual pip installs on any platform.
# GPU acceleration (optional): after pip install -r requirements.txt
# run: pip install torch==2.5.1+cu121 --extra-index-url https://download.pytorch.org/whl/cu121

numpy==2.4.4
scipy==1.17.1
pandas==3.0.3
scikit-learn==1.8.0
sentence-transformers==5.5.1
torch==2.5.1
matplotlib==3.10.7
requests==2.31.0
tqdm==4.66.1
pdfplumber==0.10.4
python-dotenv==1.2.2
zstandard==0.25.0
datasets==2.19.1
